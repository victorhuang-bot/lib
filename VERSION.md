# V19.2.26 — 司機多路線單機作業版

## 核心
同一位司機若同日負責多條路線，例如 2 線與 6 線：
- 只需要一支手機。
- 只需啟用裝置一次。
- 只需輸入 PIN 登入一次。
- 登入後直接看到「今日負責路線」切換區。
- 點 2 線只顯示 2 線分館；點 6 線只顯示 6 線分館。
- 各路線配送進度、待補簽與司機路線簽名各自獨立。
- 2 線簽名後可直接切到 6 線繼續，不需重新登入或掃 QR。

## UI
- 上方固定顯示目前作業路線。
- 每條路線按鈕顯示「完成站數 / 總站數」及是否已完成路線簽名。
- 切換路線時只顯示該線站點，降低跨線誤填。
- 路線簽名區只顯示目前選定路線。

## 其他修正
- 司機端隨車物品名稱同步支援 V19.2.24 新分類。
- 修正 driver.html 的 fmtDateTime script 放置位置。
- 完整保留 V19.2.25 隨車物品外顯與「未填」文字，以及前面所有功能。
