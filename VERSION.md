# V19.2.19 Gmail API — 歷史路線 PNG 下載修正

## 問題
V19.2.18 的「下載路線 PNG」會呼叫 `loadImg()` 載入：
- 分館簽名
- 司機簽名
- 總館簽核簽名

但 V19.2.18 並沒有定義 `loadImg()`，因此瀏覽器出現：

`loadImg is not defined`

導致 PNG 無法下載。

## V19.2.19 修正
1. 新增共用 `loadImg(src)`。
2. 支援 data URL 簽名圖與同網域圖片。
3. 單一歷史簽名圖若損壞或不存在，會回傳 null 並在 PNG 中顯示「—」，
   不會讓整個路線 PNG 下載失敗。
4. 圖片載入設 5 秒保護 timeout，避免歷史壞圖讓下載一直卡住。
5. 外部圖片若出現，使用 anonymous CORS 嘗試載入。
6. 下載錯誤訊息改為「下載路線 PNG 失敗：...」，方便後續定位。

完整保留 V19.2.18：
- 歷史日總表 PNG
- 單一路線 PNG
- 歷史路線 / 簽收查詢
- 原始簽名 / Audit Log
- 前面所有物流與 Gmail API 功能
