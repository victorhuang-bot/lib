# V18.3.4 PostgreSQL Stable Edition

基底：V18.3.3 PostgreSQL

## 本版修正

### 1. 登入 / 502 啟動穩定性
- 移除啟動時「直接刪除舊司機」的高風險 migration。
- 舊 demo/test 司機改為停用，不破壞歷史關聯。
- 啟動時個別確認 `moving` 與 `lib` 帳號存在；缺少才建立，不覆蓋既有密碼。
- Neon `DATABASE_URL`、PostgreSQL 架構維持不變。

### 2. 今日即時看板司機強制校正一次
- 路線1：許春芳
- 路線2：陳錦隆
- 路線3：彭運土
- 路線4：林聖原
- 路線5：張閔傑
- 路線6：陳錦隆
- 本 migration 只強制校正一次今天既有的舊指派，之後管理者仍可臨時改派。
- 未來每日新建路線仍自動套用上述預設司機。

### 3. 即時看板路線篩選
- 卡片改顯示 `路線1｜許春芳` 等完整名稱。
- 點「查看本路線」後，分館即時狀態只顯示該路線。
- 再點同一路線一次恢復「全部路線」。
- 分館列表路線欄顯示 `路線1`～`路線6`。

### 4. 保留
- 支援司機二次確認
- 管理者人工改派
- V17 臨時交接
- 74 個正式分館
- PostgreSQL / Neon
