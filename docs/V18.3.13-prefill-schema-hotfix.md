# V18.3.13 公文預填 Schema Hotfix

實際 `delivery_exceptions` 表：
`service_date, branch_id, exception_type, reason, created_by, created_at`

V18.3.12 錯誤使用 `start_date/end_date`，造成 PostgreSQL `UndefinedColumn`。

V18.3.13 已改成依 `service_date` 一次讀取當日 STOP / ADD。
