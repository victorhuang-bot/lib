# V18.2 SMTP Hotfix 驗證

1. 部署 V18.2 並等待 Render 顯示 Live。
2. Render → Environment：確認 `SMTP_PASSWORD` 有值。若 V18.1 已清掉，重新輸入 Google 16 位 App Password。
3. 後台 → Email 應顯示：
   - SMTP：smtp.gmail.com:587
   - 登入：victor.huang@moving-match.com
   - 寄件者：lib@moving-match.com
   - TLS：開啟
   - 密碼：已設定
   - 狀態：SMTP 設定完整
4. 輸入測試收件信箱，按「寄送測試郵件」。
5. 成功後再按「寄送今日報表 Excel + PDF」。
